- **分层结构**：`docs/` 承载长期稳定的架构说明（如 `architecture-v2.md`）与全栈学习笔记；`openspec/` 采用 spec-driven 模式管理短期变更提案（Proposal → Design → Tasks → Specs）。
- **证据驱动**：通过 `superpowers/plans` 和 `specs` 目录将高层设计转化为可执行的任务清单与验收标准。
- **配置化规范**：利用 `openspec/config.yaml` 定义 AI 辅助开发的上下文约束，确保生成内容与项目技术栈（FastAPI/Vue3）保持一致。