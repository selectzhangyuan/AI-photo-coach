- **分层结构**：`docs/` 承载宏观架构（`architecture-v2.md`）、全栈学习笔记及生产运维计划；`openspec/` 采用 spec-driven 模式管理具体功能变更（如 `add-user-auth`）。
- **内容边界**：`docs/superpowers` 聚焦生产部署与运维细节；`openspec/changes` 包含从提案（proposal）、设计（design）到任务（tasks）的完整闭环。
- **配置入口**：`openspec/config.yaml` 定义规范驱动的开发模式，指导 AI 辅助生成符合项目约定的技术文档。