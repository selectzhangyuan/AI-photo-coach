- 采用 Docker Compose 定义本地与生产两套独立的服务拓扑，涵盖 PostgreSQL、Redis、MinIO、API 及 Worker 组件。
- 通过 `nginx/default.conf` 实现前端静态资源托管与 API 请求的反向代理，统一入口流量。
- `deploy.sh` 作为部署入口，强制校验 `.env.prod` 环境变量文件完整性并执行容器化构建与启动。
- 依赖管理上，应用服务（API/Worker）严格等待基础中间件（DB/Cache/Storage）健康检查通过后启动。