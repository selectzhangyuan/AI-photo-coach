- 采用分层架构：底层为持久化存储（PostgreSQL, Redis, MinIO），中层为业务逻辑（API, Worker），顶层为 Web 前端。
- 通过 `docker-compose.yml` 定义开发环境，`docker-compose.prod.yml` 定义生产环境，实现环境隔离。
- `nginx/default.conf` 作为入口网关，处理静态资源服务及 API 请求转发。
- `deploy.sh` 封装了生产环境的依赖检查、环境变量验证及服务启动流程，确保部署一致性。